# Copy to docker/.env and set values before running docker compose.
# Used for Postgres and DATABASE_URL overrides inside containers.

POSTGRES_USER=rag
POSTGRES_PASSWORD=change-me
POSTGRES_DB=ragdb

# Optional port overrides
# POSTGRES_PORT=5432
# BACKEND_PORT=8000
# FRONTEND_PORT=3000
